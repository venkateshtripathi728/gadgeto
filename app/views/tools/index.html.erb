
<div class="category-tools">
   <div class="category btn-flat" >
     Gardening 👨‍🌾
   </div>

   <div class="category btn-flat" >
     Painting 👨‍🎨
   </div>

   <div class="category btn-flat" >
     Large works 👷‍♂️
   </div>

   <div class="category btn-flat" >
     Mechanics 🧰
   </div>

   <div class="category btn-flat" >
     Plumbing 🤿
   </div>
   <div class="category btn-flat" >
     Electricity ⚡️
   </div>
</div>

<div class="line"></div>

<div class="product-tools">
  <% @tools.each do |tool| %>
  
  <div class ="product-cards">
  <%= link_to tool_path(tool), class: "product-cards-link" do %>
    <div class="product-infos">
        <% if tool.photo.attached? %>
          <%= cl_image_tag(tool.photo.key)%>
         <%else %>
          <%=  cl_image_tag("ylqaiehw0slcqpsbivst.png") %>
           <%end %>
        <h2><%=  tool.tool_name.capitalize %></h2>
        <div class="d-flex justify-content-between m-3">
        <p><%=tool.tool_description.capitalize %></p>
        <h1><%= tool.price.to_s+"€"%> / Day</h1>
       </div>
    </div>
    <% end %>
  </div>
  
 <% end %>
</div>
