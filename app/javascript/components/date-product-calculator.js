const dates = document.querySelector("#range_start");
dates.addEventListener("change", (event) => {
      const pricePerDay = document.querySelector("#price");
      const numberOfDayss = document.querySelector("#counter");
      const totalPrice = document.querySelector("#totalprice");
      numberOfParticipants.innerHTML = participants;
      participants += 1;
      numberOfParticipants.innerHTML = participants;
      }

      totalPrice.value =  ${price * numberOfDayss}€`;
    });
 
  